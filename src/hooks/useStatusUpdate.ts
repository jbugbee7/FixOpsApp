export const useStatusUpdate = () => {
  const handleStatusUpdate = async () => {};
  const handleSPTComplete = async () => {};
  
  return {
    updateStatus: async () => {},
    handleStatusUpdate,
    handleSPTComplete
  };
};
